<%= form_for(@default_calendar) do |f| %>
  <% if @default_calendar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@default_calendar.errors.count, "error") %> prohibited this default_calendar from being saved:</h2>

      <ul>
      <% @default_calendar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :anio %><br>
    <%= f.number_field :anio %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
