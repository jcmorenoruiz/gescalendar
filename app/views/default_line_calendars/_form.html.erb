<%= form_for(@default_line_calendar) do |f| %>
  <% if @default_line_calendar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@default_line_calendar.errors.count, "error") %> prohibited this default_line_calendar from being saved:</h2>

      <ul>
      <% @default_line_calendar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
