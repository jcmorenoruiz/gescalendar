en:
  errors:
    messages:
      invalid_date: "No es una fecha válida"
      invalid_time: "La hora no es correcta"
      invalid_datetime: "No es una vecha válida"
      is_at: "debe ser %{restriction}"
      before: "debe ser anterior a %{restriction}"
      on_or_before: "debe ser igual o anterior a %{restriction}"
      after: "debe ser posterior %{restriction}"
      on_or_after: "debe ser igual o posterior a %{restriction}"
  validates_timeliness:
    error_value_formats:
      date: '%d-%m-%Y'
      time: '%H:%M:%S'
      datetime: '%d-%m-%Y %H:%M:%S'
